interface ListItem {
  id: string;
  title: string;
  url: string;
  favIcon: string;
}

interface LinkItem {
  id: string;
  title: string;
  url: string;
  favIcon: string;
}

export { ListItem, LinkItem };
